<Page x:Class="ShoesNet.Wndows.OrdersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="900"
      MinWidth="900" MinHeight="600"
      Title="Заказы" Loaded="Page_Loaded">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="BtnAddOrder"
                    Content="Добавить заказ"
                    Visibility="Collapsed"
                    Click="BtnAddOrder_Click"/>
        </StackPanel>

        <ListView x:Name="LViewOrders"
                  Grid.Row="1"
                  SelectionMode="Single"
                  HorizontalContentAlignment="Stretch"
                  MouseLeftButtonUp="LViewOrders_MouseLeftButtonUp"
                  ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border x:Name="OrderBorder"
                            BorderBrush="Black"
                            BorderThickness="2"
                            Margin="5"
                            Padding="12"
                            Height="180"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Top">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="210"/>
                                
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Orientation="Vertical">
                                <TextBlock Text="Артикул заказа" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Артикул}" Margin="0,0,0,6"/>

                                <TextBlock Text="Статус заказа" FontWeight="Bold"/>
                                <TextBlock Text="{Binding СтатусЗаказа}" Margin="0,0,0,6"/>

                                <TextBlock Text="Адрес пункта выдачи (текст)" FontWeight="Bold"/>
                                <TextBlock Text="{Binding АдресПунктаВыдачи}" TextWrapping="Wrap" Margin="0,0,0,6"/>

                                <TextBlock Text="Дата заказа" FontWeight="Bold"/>
                                <TextBlock Text="{Binding ДатаЗаказа}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Orientation="Vertical" Margin="10,0,0,0">
                                <TextBlock Text="Дата доставки" FontWeight="Bold"/>
                                <TextBlock Text="{Binding ДатаДоставки}" Margin="0,6,0,0"/>

                                <Button x:Name="BtnDelete"
                                        Content="Удалить"
                                        Background="#00FA9A"
                                        Foreground="White"
                                        Loaded="BtnDelete_Loaded"
                                        Click="BtnDelete_Click"
                                        Margin="0,14,0,0"
                                        Padding="6,2"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>

