<Page x:Class="ShoesNet.Wndows.AddEditOrderPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      MinWidth="900" MinHeight="600"
      Title="Добавление заказа">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                BorderBrush="Black"
                BorderThickness="2"
                Padding="12"
                CornerRadius="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="200"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Column="0" Orientation="Vertical" Margin="0,0,16,0">
                    <TextBlock Text="Артикул заказа" Margin="0,0,0,5"/>
                    <TextBox x:Name="TxtArticle" Margin="0,0,0,10"/>

                    <TextBlock Text="Статус заказа" Margin="0,0,0,5"/>
                    <ComboBox x:Name="CmbStatus" Margin="0,0,0,10"/>

                    <TextBlock Text="Адрес пункта выдачи (текст)" Margin="0,0,0,5"/>
                    <TextBox x:Name="TxtPickupAddress" Margin="0,0,0,10"/>

                    <TextBlock Text="Дата заказа" Margin="0,0,0,5"/>
                    <DatePicker x:Name="DpOrderDate" Margin="0,0,0,10"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Vertical">
                    <TextBlock Text="Дата доставки" Margin="0,0,0,5"/>
                    <DatePicker x:Name="DpPickupDate" Margin="0,0,0,10"/>
                </StackPanel>
            </Grid>
        </Border>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,20,0,0">
            <Button Content="Сохранить" Click="BtnSave_Click" Width="180"/>
        </StackPanel>
    </Grid>
</Page>

