<Page x:Class="ShoesNet.Wndows.CatalogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ShoesNet.Wndows"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Каталог товаров" Loaded="Page_Loaded">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="TxtSearch" TextChanged="FilterData" Grid.Column="0" Margin="5" ToolTip="Поиск по названию"/>
            <ComboBox x:Name="CmbSort" SelectionChanged="FilterData" Grid.Column="1" Margin="5">
                <ComboBoxItem Content="Без сортировки" IsSelected="True"/>
                <ComboBoxItem Content="По возрастанию цены"/>
                <ComboBoxItem Content="По убыванию цены"/>
            </ComboBox>
            <ComboBox x:Name="CmbFilter" SelectionChanged="FilterData" Grid.Column="2" Margin="5">
            </ComboBox>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock x:Name="TxtCount" VerticalAlignment="Center" FontWeight="Bold" Margin="5,0,20,0"/>
            <TextBlock x:Name="TxtUserName" VerticalAlignment="Center" Margin="0,0,20,0"/>
            <Button x:Name="BtnAddProduct" Content="ДобавитьТовар" Visibility="Collapsed"/>
            <Button x:Name="BtnOrders" Content="Заказы" Visibility="Collapsed"/>
        </StackPanel>

        <ListView x:Name="LViewProducts" Grid.Row="2" ScrollViewer.HorizontalScrollBarVisibility="Disabled" MouseDoubleClick="LViewProducts_MouseDoubleClick">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border x:Name="ProductBorder" BorderThickness="1" BorderBrush="LightGray" Margin="5" Padding="5" Width="800">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="160"/>
                            </Grid.ColumnDefinitions>

                            <Image Source="{Binding ПолныйПутьКФото}" Width="80" Height ="80"/>

                            <StackPanel Grid.Column="1" Margin="10,0">
                                <TextBlock Text="{Binding НаименованиеТовара}" FontWeight="Bold" FontSize="16"></TextBlock>
                                <TextBlock Text="{Binding ОписаниеТовара}" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Производитель, StringFormat='Производитель: {0}'}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                <TextBlock Text="{Binding Цена, StringFormat='Цена: {0} руб.'}" FontWeight="Bold" FontSize="18"/>
                                <TextBlock Text="{Binding ДействующаяСкидка, StringFormat='Скидка: {0}%'}"/>
                                <Button x:Name="BtnDelete" Content="Удалить" Background="Red" Foreground="White" 
                                    Loaded="BtnDelete_Loaded" Click="BtnDelete_Click"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding ДействующаяСкидка, Converter={StaticResource DiscountConverter}}" Value="True">
                            <Setter TargetName="ProductBorder" Property="Background" Value="#2E8B57"/>
                        </DataTrigger>
                    </DataTemplate.Triggers>
                   </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
